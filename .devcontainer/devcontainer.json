// for format details, see https://aka.ms/vscode-remote/devcontainer.json
{
	"name": "Node",
	"build": {
		"dockerfile": "Dockerfile",
		"context": "..",
		"args": {
			"USER": "node"
		}
	},
	"remoteUser": "node"
}